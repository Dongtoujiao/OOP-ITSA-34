#include <bits/stdc++.h>
using namespace std;

int main(){

    int n, a;
    cin >> n;
    while(n--){
        cin >> a;
        cout << a << " " << a * a << " " << a * a * a << "\n";
    }

    return 0;
}